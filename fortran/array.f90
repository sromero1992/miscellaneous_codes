program arrayP

      real   numbers(5) ! 1D array
      integer   mat(3,3),i,j ! 3D array

      do i=1,5
        print *, numbers(i)=i*2
      end do
        print *, numbers(i)
       ! do i=1,3
       !         do j=1,3
        !        mat(i,j) = i + j
        !        end do
       ! end do
       ! print *, mat(i,j)

        ! short hand assignment
       ! numbers= (/1.5, 3.2,4.5,1.1,5.6/)
      ! print *, numbers(i)   

 end
     
      
